<!-- general form elements disabled -->
          <div class="box box-warning">
            <div class="box-header with-border">
              <h3 class="box-title"><%= @label %> Orden</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
            <%= form_for(@user) do |f| %>
     
     <% if @user.errors.any? %>
    
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this invoice from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
            <!--  <form role="form"> -->
              <!-- textarea -->
                <div class="form-group">
                  <label>Name</label>
                  <%= f.text_field :name, class: "form-control", row: "3" %>
                </div>

                 <!-- textarea -->
                <div class="form-group">
                  <label>Email</label>
                  <%= f.email_field :email, class: "form-control", row: "3" %>
                </div>

                <!-- text input -->
                <div class="form-group">
                  <label>Password</label>
                <%= f.password_field :password, class: "form-control" %>
                                </div>
                
                <%= f.submit @button, class: "btn btn-sm btn-info btn-flat pull-left" %>
                <%= link_to('Volver', users_path, class: "btn btn-sm btn-info btn-flat pull-right") %>
               
                <% end %>
              <!-- </form> -->
            </div>
            </div>